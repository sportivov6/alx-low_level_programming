/*
 * 2-print_aplhabet.c
 */

#include <stdio.h>

/**
 * main program prints the alphabet in lowercase
 */

int main (void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
		putchar(letter)

	putchar('\n');

	return (0);
}
